/*
   CAPACITY        |             DESCRIPTION
___________________|________________________________________________
   size            |     Return length of string (public) strlen()
   length          |     Return length of string (public) strlen()
   max size        |     Return maximum size of string (public) 
   resize          |     Resize string (public)
   capacity        |     Return size of allocated storage (public) 
   reserve         |     Request a change in capacity (public)
   clear           |     Clear string (public) strcpy() 
   empty           |     Test if string is empty (public)
   shrink_to_fit   |     Shrink to fit (public)
___________________|_________________________________________________

*/

#include <iostream>
#include <string>
using namespace std;
 
int main(){
 
    
 
return 0;
}