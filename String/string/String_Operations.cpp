/*
  String Operations |
____________________|________________________________________________________________
       c_str        |   Get C string equivalent (public) 
       data         |   Get string data (public)
       get          |   allocator Get allocator (public)
       copy         |   Copy sequence of characters from string (public) 
       find         |   Find content in string (public) 
       rfind        |   Find last occurrence of content in string (public)
  find_first_of     |   Find character in string (public) 
  find_last_of      |   Find character in string from the end (public) 
 find_first_not_of  |   Find absence of character in string (public)
 find_last_not_of   |   Find non-matching character in string from the end (public)
      substr        |   Generate substring (public) 
      compare       |   Compare strings (public)
______________________________________________________________________________________

*/

#include <iostream>
#include <string>
using namespace std;
 
int main(){
 
    
 
return 0;
}